<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/product.css" />
    <title>Delete Product</title>
  </head>
  <body class="product-body">
    <!-- Delete Product Form -->
    <form action="/delete" method="post" class="product-form">
      <!-- Error and success messages -->
      <% if (errorMessage) { %>
      <div class="error-message" style="color: red; margin-bottom: 1em" ,>
        <p><%= errorMessage %></p>
      </div>
      <% } %> <% if (successMessage) { %>
      <div class="success-message" style="color: green; margin-bottom: 1em" ;>
        <p><%= successMessage %></p>
      </div>
      <% } %>
      <h2 class="product-heading">Delete Product</h2>
      <label for="pID" class="product-header-label">
        Are you sure you want to delete this Product?
      </label>
      <div class="product-form-body">
        <label for="pID" class="product-label">Enter Product ID:</label>
        <input type="text" id="pID" name="pID" class="product-input" required />
      </div>
      <button type="submit" class="product-btn">Delete</button>
    </form>
  </body>
</html>
